# Student_Database_Using_File_Handling

The Student Management System is a Python program that allows users to store and manage student records using a CSV (Comma-Separated Values) file. It provides basic functionalities such as adding new student details, viewing all records, and searching for a specific student.

The program uses a file named students(2).csv to save data permanently, so the records are not lost after the program ends.
